<%= form_for(stock_option) do |f| %>
  <% if stock_option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stock_option.errors.count, "error") %> prohibited this stock_option from being saved:</h2>

      <ul>
      <% stock_option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
